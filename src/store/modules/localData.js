const state = {
  
};

const mutations = {
  
};

const actions = {
  getFrom: (state) => {
    return state.from;
  }
};

const getters = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
};
